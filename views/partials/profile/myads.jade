append stylesheets
    link(rel='stylesheet', type='text/css', href='semantic/dist/components/item.min.css')

h2(ng-if="myAds.length > 0  ") My Ads
h2.text-center(ng-if="myAds.length < 1") No Results

.ui.segment.no-segment
    .ui.divided.items
        .item(ng-repeat="ad in myAds")
            a.image.myAdsList(href='/detail/{{ ad.slug }}/{{ ad._id }}', target="_blank")
                img(ng-src='{{ amazon_base_url }}/{{ ad.photos[ad.photoShowcaseIndex].filename }}', ng-show="{{ ad.photos !== null }}")
                img(ng-src='/img/no-photo.png', ng-show="{{ ad.photos === null }}")
            .content
                a.header(href='/detail/{{ ad.slug }}/{{ ad._id }}' , target="_blank")
                    | {{ ad.title }}
                .meta
                    h5.cinema ${{ad.price}}
                .description
                    p
                .extra
                    .ui.label(
                            ng-class="{red: ad.status===2 || ad.status === 4, orange: ad.status === 0}"
                        )
                        | {{ ad.statusText }}

                    div.margin-top-10(ng-show="ad.totalActivePower > 0")
                        h4
                            i.ui.rocket.icon
                            | {{ ad.totalActivePower }}

                    .ui.simple.dropdown.right.floated.item
                        | Settings
                        i.dropdown.icon
                        .menu
                            a.item(href="./newAd?id={{ ad._id }}")
                                i.edit.icon
                                | Edit
                            a.item
                                i.rocket.icon
                                |Buy Power
                            a.item(href="javascript:;", ng-click="unpublish(ad._id)", ng-show="ad.status === 1")
                                i.trash.icon
                                | Unpublish

.ui.dimmer(ng-class="{active: loadingMyAds, active:loadingUnpublish }")
    .ui.loader


